services:
  web:
    image: nginx:alpine
    ports:
    - "5173:80"
    volumes:
    - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    - ../recipes-ui/dist:/www:ro

  api:
    build: .
    volumes:
      - ./local-docker-config.toml:/app/config.toml


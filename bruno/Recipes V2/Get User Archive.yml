info:
  name: Get User Archive
  type: http
  seq: 9

http:
  method: GET
  url: "{{host}}/user/recipes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

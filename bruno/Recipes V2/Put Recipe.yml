info:
  name: Put Recipe
  type: http
  seq: 10

http:
  method: PUT
  url: "{{host}}/recipe/:id"
  params:
    - name: id
      value: 01KXBZQR4G05T2DE9JKQY1526H
      type: path
  body:
    type: json
    data: |-
      {
        "is_archived": {{is_archived}}
      }
  auth: inherit

runtime:
  variables:
    - name: is_archived
      value: "true"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

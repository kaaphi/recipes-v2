info:
  name: Search User Recipes
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/user/recipes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

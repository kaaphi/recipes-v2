info:
  name: Get User Recipes
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/user/recipes/:user_id"
  params:
    - name: user_id
      value: "1234"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

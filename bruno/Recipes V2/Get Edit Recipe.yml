info:
  name: Get Edit Recipe
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/recipe/edit/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
